{% if property('componentNames')|length %}
    <table class="detail-table">
    {% for componentName in property('componentNames') %}
        <tr>
            <th>{{ componentName }}</th>
            <td>
                {{ component(componentName) }}
            </td>
        </tr>
    {% endfor %}
    </table>
{% else %}
    no details found.
{% endif %}