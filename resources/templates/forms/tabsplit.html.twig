<gr-tab-group>
  {% for tab in property('tabs') %}
    <gr-tab panel="{{ tab }}">{{ tab }}</gr-tab>
  {% endfor %}
  {% for tab in property('tabs') %}
    <gr-tab-panel name="{{ tab }}">
      {{ component(tab) }}
    </gr-tab-panel>
  {% endfor %}
</gr-tab-group>