<!-- Top Bar -->
<header class="relative flex items-center justify-between gap-2 overflow-visible bg-slate-800 px-2 py-2 text-white shadow-lg z-[40] sm:px-4">

    <div class="flex flex-shrink-0 items-center gap-2 min-w-0 pl-1">

        {{ component('logo') }}
        {{ component('middle') }}


    </div>

    <div class="relative flex-shrink-0">
        {{ component('login') }}
    </div>

</header>
